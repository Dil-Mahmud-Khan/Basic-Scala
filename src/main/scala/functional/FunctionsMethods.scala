package functional

// TODO: Complete the expressions marked as ???.

object FunctionsMethods extends App {
  val values: List[Int] = List(2, 1, 4, 3, 5)
  val otherValues: List[Int] = List(6546, 323, -7637, 0)

//  val getMinMax: List[Int] => (Int, Int) = ???
//
//  def getMinMaxMethod(???): ? = ???
}
